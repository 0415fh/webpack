--progress   // 打包过程
--display-modules // 打包模块
--colors  //打包字为彩色
--display-reason //打包原因


output: {
  path: __dirname + '/dist/js',
  filename: '[name]-[hash].js'
}

output: {
  path: __dirname + '/dist/js',
  filename: '[name]-[chunkhash].js'
}
